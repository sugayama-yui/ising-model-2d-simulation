# 2次元強磁性イジング模型の相転移シミュレーション：プロジェクト概要

## 1. 目的
本プロジェクトでは、古典モンテカルロ法（メトロポリス・アルゴリズム）を用いて2次元正方格子上の強磁性イジング模型をシミュレーションし、有限サイズスケーリング解析を通じて相転移温度 $T_c$ を特定することを目的とします。得られた数値解を、オンサガーの厳密解と比較検証します。

## 2. 物理モデル
- **系**: $L \times L$ の正方格子（周期境界条件）。
- **変数**: 各サイト $i$ にスピン $s_i \in \{+1, -1\}$。
- **ハミルトニアン**: $H = -J \sum_{\langle i,j \rangle} s_i s_j$ ($J=1$)。
- **目標値**: 厳密解 $T_c = 2 / \ln(1 + \sqrt{2}) \approx 2.269$。

## 3. シミュレーション手法
- **アルゴリズム**: メトロポリス・アルゴリズムを採用。
- **更新単位 (1 MCS)**: 全サイト（$L^2$ 箇所）に対してスピン反転の試行を1回ずつ行うことを 1 モンテカルロステップ (MCS) と定義します。
- **計算量**: 1サイトの更新判定は隣接スピンのみを参照するため $O(1)$ です。1 MCS 全体では $O(L^2)$ でスケールすることを保証します。

## 4. 解析手法：有限サイズスケーリング
有限系では相転移がなだらかになるため、以下の**ビンダー累積量 (Binder Cumulant)** $U_4$ を用いて $T_c$ を求めます。
$$U_4 = 1 - \frac{\langle m^4 \rangle}{3 \langle m^2 \rangle^2}$$
異なる系サイズ $L$ について $U_4(T)$ をプロットした際、それらの曲線が交差する点が熱力学的極限（$L \to \infty$）における転移温度 $T_c$ に相当します。

## 5. 検証とテストの計画
コードの信頼性を確保するため、以下の検証を行います：
1.  **最小系 ($L=2$) でのユニットテスト**:
    全 $2^4 = 16$ 通りの状態を列挙して計算した厳密な期待値と、シミュレーション結果を比較します。
2.  **計算量の実測**:
    $L$ を変化させた際の実行時間が $L^2$ に比例して増加することを確認します。
3.  **厳密解との比較**:
    有限サイズスケーリングで得られた $T_c$ が、オンサガーの理論値（約 2.269）と一致することを確認します。

## 6. 作業ロードマップ
1.  理論背景と概要の Markdown 作成（本ドキュメント）。
2.  シミュレーションコアコード (`ising.py`) の実装。
3.  ユニットテスト (`test_ising.py`) による $L=2$ および $O(L^2)$ の検証。
4.  データ収集と解析スクリプト (`analysis.py`) による $T_c$ の推定。
5.  結果の可視化と最終報告（README の更新）。
