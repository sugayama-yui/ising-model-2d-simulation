# 2次元強磁性イジング模型の相転移に関する考察ノート

## 1. はじめに
本ノートでは、2次元正方格子上の強磁性イジング模型を古典モンテカルロ法（メトロポリス法）を用いてシミュレーションし、相転移温度 $T_c$ を求める手法についてまとめる。

## 2. 理論モデル
### ハミルトニアン
$L \times L$ の正方格子上の各サイト $i$ にスピン $s_i \in \{+1, -1\}$ を配置する。周期境界条件の下で、相互作用エネルギー（ハミルトニアン）は次のように書ける。
$$H = -J \sum_{\langle i,j \rangle} s_i s_j$$
ここで、$J > 0$ は強磁性的相互作用定数であり、$\langle i,j \rangle$ は隣接するペアについての和をとることを意味する。本稿では $J=1$, $k_B=1$ とする。

### 厳密解 (Onsagerの解)
無限系 ($L \to \infty$) における転移温度 $T_c$ は以下のように厳密に求められている。
$$T_c = \frac{2}{\ln(1 + \sqrt{2})} \approx 2.269185$$

## 3. モンテカルロ法とメトロポリス法
平衡状態における物理量の期待値を計算するために、メトロポリス・アルゴリズムを用いる。

### アルゴリズムの構成
1.  あるスピン $s_i$ をランダムに選ぶ（または全サイトを順番に回る）。
2.  そのスピンを反転させた場合のエネルギー変化 $\Delta E$ を計算する。
3.  以下の確率 $W$ で反転を受け入れる：
    $$W(s_i \to -s_i) = \min(1, \exp(-\Delta E / T))$$
4.  これを繰り返す。

### 1 モンテカルロステップ (1 MCS) の定義
「全スピンの状態更新を1回ずつ試みる」ことを 1 MCS と定義する。サイト数を $N = L^2$ とすると、$N$ 回のスピンフリップ試行が 1 MCS に相当する。

### 計算量 $O(L^2)$ の保証

#### 1. 局所更新の定数時間 $O(1)$ とは？
コンピュータで計算を行う際、$O(1)$ とは **「データの総数（系サイズ $L$）が増えても、その作業にかかる時間は変わらない」** という意味です。

なぜ $O(1)$ で済むのか？
イジング模型のエネルギー $H = -J \sum s_i s_j$ を馬鹿正直に計算しようとすると、全ペアの計算が必要になり、1回のスピン反転ごとに膨大な計算量（$O(L^2)$）がかかってしまいます。

しかし、メトロポリス法では「反転した時の差分（$\Delta E$）」だけを見れば良いというルールがあります。
- スピン $s_i$ を反転させたとき、その影響を受けるのはそのスピンと直接つながっている隣接サイトだけです。
- 2次元正方格子の場合、隣接サイトは常に「上・下・左・右」の **4つ** だけです。

計算のイメージ：
- $L=10$（全100サイト）の時：隣接4サイトを見て計算。
- $L=100$（全10,000サイト）の時：隣接4サイトを見て計算。
- $L=1000$（全1,000,000サイト）の時：隣接4サイトを見て計算。

このように、全体のサイズ $L$ がどれだけ巨大になっても、「1回の更新試行でチェックする相手は常に4人だけ」 なので、計算コストは一定（定数時間 $O(1)$）となります。

#### 2. 全体のスケーリング $O(L^2)$ とは？
1回の試行が $O(1)$ であることがわかったところで、次に「1 MCS（1ステップ）」全体の時間を考えます。

**面積に比例する計算量**
1 MCS は「全サイトが平均1回更新を試みる」ステップなので、合計 $L^2$ 回の試行を繰り返します。
- 1回の試行のコスト = $C$（定数）
- 試行回数 = $L^2$
- 1 MCS の総コスト = $C \times L^2$

これをオーダー記法で書くと $O(L^2)$ となります。つまり、「計算時間は格子の面積（サイト数）にきれいに比例する」 ということです。

**具体例で考えるスケーリングの威力**
例えば、系の1辺の長さ $L$ を2倍（$L \to 2L$）にしたとします。
- サイト数は $L^2$ から $(2L)^2 = 4L^2$ に、つまり **4倍** になります。
- $O(L^2)$ のアルゴリズムなら、計算時間も正確に **4倍** になります。

もしこれが $O(L^4)$ のような効率の悪いアルゴリズムだった場合、$L$ を2倍にすると計算時間は $2^4=16$ 倍になってしまいます。$O(L^2)$ であることは、巨大な系を現実的な時間でシミュレーションするために不可欠な性質なのです。

## 4. 有限サイズスケーリング (FSS)
有限の $L$ では相転移は厳密には起こらず、物理量は滑らかに変化する。真の $T_c$ を推定するために、以下の物理量に着目する。

### ビンダー累積量 (Binder Cumulant)
$$U_4 = 1 - \frac{\langle m^4 \rangle}{3 \langle m^2 \rangle^2}$$
ここで $m = \frac{1}{N} \sum s_i$ は磁化である。
- $T > T_c$: スピンがバラバラで $m$ の分布がガウス分布に近づくため、$U_4 \to 0$ ($L \to \infty$)。
- $T < T_c$: スピンが揃い $m \approx \pm 1$ となるため、$U_4 \to 2/3$ ($L \to \infty$)。
- $T = T_c$: $U_4$ は $L$ によらない一定値をとる。

したがって、異なる $L$ に対して $U_4(T)$ をプロットし、その交点を求めることで $T_c$ を高精度に推定できる。

### その他の物理量
- **帯磁率 $\chi$**: $\chi = \frac{N}{T} (\langle m^2 \rangle - \langle |m| \rangle^2)$。$T_c$ 付近でピークを持ち、その高さは $L^{\gamma/\nu}$ で発散する。
- **比熱 $C_v$**: $C_v = \frac{1}{N T^2} (\langle E^2 \rangle - \langle E \rangle^2)$。

## 5. 実装上の注意点
- **熱浴法とメトロポリス法**: 今回はメトロポリス法を用いる。
- **緩和時間**: 初期状態から平衡状態に達するまで十分なステップ（Sweeps）を捨てる必要がある。
- **相関時間**: サンプリング間の相関を避けるため、一定間隔おきにデータを取得する。
- **乱数**: 高品質な乱数生成器を使用する。

## 6. 検証方法
- $L=2$ の場合、全 $2^{2 \times 2} = 16$ 通りの状態をすべて列挙して統計量を計算できる。これとシミュレーション結果を比較することで、コードの正当性を確認する。
- 計算時間が $L^2$ に比例することを実測し、計算量のスケーリングを確認する。
